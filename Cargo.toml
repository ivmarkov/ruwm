[workspace]
resolver = "2"

members = [
    "ruwm",
    "ruwm-esp32",
]

#[patch.crates-io]
#esp-idf-svc = { path = "../esp-idf-svc" }

[profile.release]
opt-level = "s"

[profile.dev]
debug = true # Symbols are nice and they don't increase the size on Flash
opt-level = "z"
